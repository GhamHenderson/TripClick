<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>

<body>
    <h1>
        <% if(error.length > 0 ) { %>
            <div class="alert alert-danger">
                <%= error %>
            </div>
        <% } %>
    </h1>

</body>
</html>